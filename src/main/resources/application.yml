spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  cloud:
    zookeeper:
      connect-string: localhost:2181

management:
#  server:
#    port: 8081
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

info:
  app:
    name: waiter-service
    author: "Kevin Jin"

server:
  port: 0

#server:
#  compression:
#    enabled: true
#    min-response-size: 512B
#  error:
#    include-stacktrace: always
#    include-exception: true
